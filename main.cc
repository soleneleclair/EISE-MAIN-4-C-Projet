#include <stdio.h>
#include <iostream>
#include "Utilisateur.hh"
#include "Eau.hh"
#include "Gourde.hh"

int main()
{
  Eau pg;
  pg.print();

  //Utilisateur test1;
  //std::cout << test1.get_recherche() << std::endl;
  // Utilisateur user1("Braunstein","Cécile",2);
  // Utilisateur user2(user1);
  //
  // std::cout << user1.get_recherche() << std::endl;
  // std::cout << user2.get_recherche() << std::endl;

  Gourde gourde1;
  Gourde gourde2;
  Gourde gourd;
  Gourde gour;
  Gourde gou;
  // gourde1.calcul(pg);
	return 0;
}
